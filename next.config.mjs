/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // true by default, to prevent re-runs of useEffect, it has to be false in dev mode
};

export default nextConfig;
